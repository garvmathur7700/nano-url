# Tell Hibernate to build the tables based on your @Entity classes
spring.jpa.hibernate.ddl-auto=create-drop

# Optional but helpful: Show the SQL in the logs so you can see it working
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true